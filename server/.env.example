# Admin dashboard password
ADMIN_PASSWORD=admin123

# Server port (local)
PORT=3001

# Excel files are written to server/exports/ (local) or Netlify Blobs (production)
# No Google API or cloud sheet setup required.